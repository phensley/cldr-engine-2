{
  "name": "lazy-app",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "S2 consumer example: open locale set (lazy mode). cldr.config.ts → generated cldr.gen.ts with a literal per-locale import() map; preload seam before get().",
  "license": "Apache-2.0",
  "dependencies": {
    "@phensley/cldr": "workspace:*",
    "@phensley/cldr-generate": "workspace:*"
  }
}
